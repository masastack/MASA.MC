@inherits AdminCompontentBase
<MListItem>
    <MListItemContent>
        <MListItemTitle Class="body regular--text">@Value?.ToString(T("$DateFormat"))</MListItemTitle>
        <MListItemSubtitle Class="body2 regular3--text">@Value?.ToString(T("$TimeFormat"))</MListItemSubtitle>
    </MListItemContent>
</MListItem>

@code {
    [Parameter]
    public DateTimeOffset? Value { get; set; }
}
