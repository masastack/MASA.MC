@inherits AdminCompontentBase
<MRow>
    <MCol Md="6">
        <MTextField @bind-Value="_model.AccessKeyId"
                    Label="@T("ChannelSmsOptions:AccessKeyId")"
                    Dense
                    Outlined
                    HideDetails="@("auto")"
                    TValue="string"
                    OnChange="HandleChangeAsync"/>
    </MCol>
    <MCol Md="6">
        <MTextField @bind-Value="_model.AccessKeySecret"
                    Label="@T("ChannelSmsOptions:AccessKeySecret")"
                    Dense
                    Outlined
                    HideDetails="@("auto")"
                    TValue="string"
                    OnChange="HandleChangeAsync"/>
    </MCol>
</MRow>