@inherits AdminCompontentBase
<div @ref="Ref">
    <SheetDialog Value="@_visible" ValueChanged="HandleVisibleChanged" Title="@T("Permission.EditReceiverGroup")">
        <MForm Model="_model" EnableValidation>
            <div class="sheet-dialog full-height flex-column">
                <div class="mt-6 flex-height-auto overflow-y">
                    <MRow Justify="@("center")">
                        <MCol Cols="12" Sm="6" Md="3">
                            <MTextField Class="mx-auto" style="width:200px;" @bind-Value="_model.DisplayName" HideDetails="@("auto")" Placeholder="@T("DisplayName.ReceiverGroupDisplayName")"></MTextField>
                        </MCol>
                    </MRow>
                    <ReceiverSelect @bind-Value="_model.Items"></ReceiverSelect>
                    <MRow>
                        <MCol Cols="12">
                            <MTextarea @bind-Value="_model.Description"
                                       Label="@T("DisplayName.ReceiverGroupDescription")"
                                       Dense
                                       Outlined
                                       HideDetails="@("auto")">
                            </MTextarea>
                        </MCol>
                    </MRow>
                </div>
                <div Class="d-flex mt-6">
                    <MButton Class="mr-6" Text Color="error" OnClick="HandleDel">@T("Delete")</MButton>
                    <MSpacer></MSpacer>
                    <MButton Color="primary" Class="small-button" OnClick="async() => await HandleOk(context)">@T("Save")</MButton>
                </div>
            </div>
        </MForm>
    </SheetDialog>
</div>
